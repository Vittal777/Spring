pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Source code will be checked out by Jenkins'
            }
        }

        stage('Build') {
            steps {
                echo 'Building application'
            }
        }
    }
}